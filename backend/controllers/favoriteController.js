const LocationModel = require('../models/Location');
const EventModel = require('../models/Event');

class favoriteController {
    static async addLocationToFavorites(req, res) {
        /**
         * 1. Receive request to add a location to user's favorites
         * 2. Validate input parameters
         * 3. Use model functions to update user's favorite locations in the database
         * 4. Return success response to the client
         * 5. Capture and deal with errors
         */
    }
}

module.exports = favoriteController;
